//... click for uoload files
const wrapper = document.querySelector(".wrapper"),
    fileInput = document.querySelector(".file-input");

wrapper.addEventListener("click", () => {
    fileInput.click();
});